# ============================================
# Environment Variables Template
# ВАЖНО: Скопируй в .env и заполни настоящими значениями!
# ============================================
# 
# Команда: cp .env.example .env
# Затем: nano .env  (и заполни секреты)
#

# === PostgreSQL Database ===
POSTGRES_DB=smart_money_db
POSTGRES_USER=trader
POSTGRES_PASSWORD=CHANGEME_super_secret_password_here

# WHY: Используется docker-compose.yml для инициализации базы
# ВАЖНО: Смени пароль в production!


# === Binance API (обязательно) ===
BINANCE_API_KEY=your_binance_api_key_here
BINANCE_API_SECRET=your_binance_api_secret_here

# WHY: Для WebSocket подключения к Binance (торговля, стакан, сделки)
# Получить: https://www.binance.com/en/my/settings/api-management
# Права: "Enable Reading" (НЕ нужны права на торговлю для read-only режима)


# === Deribit API (опционально - для GEX данных) ===
DERIBIT_API_KEY=
DERIBIT_API_SECRET=

# WHY: Для интеграции Gamma Exposure (опционы)
# Получить: https://www.deribit.com/main#/account/apiKeys
# Опционально - можно оставить пустым если не используешь GEX


# === Application Settings ===
LOG_LEVEL=INFO
# Варианты: DEBUG, INFO, WARNING, ERROR, CRITICAL
# WHY: DEBUG для разработки, INFO для production

ENVIRONMENT=production
# Варианты: development, production
# WHY: Влияет на поведение логирования и error handling


# === Optional: pgAdmin (если раскомментировать в docker-compose.yml) ===
# PGADMIN_EMAIL=admin@smartmoney.local
# PGADMIN_PASSWORD=admin


# ============================================
# SECURITY BEST PRACTICES
# ============================================
#
# 1. НИКОГДА не коммить .env в Git!
#    Добавь в .gitignore: echo ".env" >> .gitignore
#
# 2. Используй strong пароли для POSTGRES_PASSWORD:
#    openssl rand -base64 32
#
# 3. API ключи Binance:
#    - Создавай отдельные ключи для разных серверов
#    - Ограничь права только чтением (no trading/withdrawal)
#    - Включи IP Whitelist если возможно
#
# 4. На Oracle Cloud:
#    chmod 600 .env  # Только владелец может читать
#
# ============================================
