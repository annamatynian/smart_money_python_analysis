# GEMINI VALIDATION: Терминологическая путаница Whale/Dolphin/Shark

**Дата:** 2025-12-23  
**Валидатор:** Gemini  
**Статус:** ✅ КРИТИЧНЫЕ ПРОБЛЕМЫ ОБНАРУЖЕНЫ

---

## 🚨 ВЕРДИКТ GEMINI: ТРЕБУЕТСЯ РЕФАКТОРИНГ

### ВОПРОС 1: Создаёт ли путаницу в SmartCandles?

**ОТВЕТ:** ДА, ДЛЯ ЧЕЛОВЕКА. НЕТ, ДЛЯ КОДА.

- **Для кода:** PostgreSQL и Python всё равно. Колонки работают.
- **Для человека:** Когнитивный диссонанс. "Dolphin (агрессор) продавливает Shark (стену)" — мозг споткнётся.
  - Почему акула (хищник) — это жертва?
  - Почему дельфин (меньше акулы) сильнее акулы?

**РЕЗУЛЬТАТ:** Ошибки в интерпретации сигналов и баги при генерации фичей.

---

### ВОПРОС 2: Переименовать is_shark → is_dolphin?

**ОТВЕТ:** ДА. СИНХРОНИЗИРУЙТЕ КЛАССЫ РАЗМЕРОВ.

**Золотой стандарт аналитики:** Разделять **Роль (Role)** и **Размер (Size)**

**Размер (Size Tier)** — должен быть единым для всего проекта:
- **Whale** (> $100k)
- **Dolphin** ($1k - $100k) ← Бывшая Акула
- **Minnow** (< $1k)

**Роль (Role):**
- **Aggressor** (CVD / Taker) — бьёт по рынку
- **Passive** (Iceberg / Limit / Maker) — стоит стеной

**РЕШЕНИЕ:** Переименовать `is_shark` → `is_dolphin` в `domain.py`, даже если айсберги обычно крупнее $10k.

---

### ВОПРОС 3: Может ли ML спутать dolphin CVD с dolphin iceberg?

**ОТВЕТ:** НЕТ, ЕСЛИ ИМЕНА КОЛОНОК РАЗНЫЕ.

ML модель (XGBoost/LSTM) видит не слова, а векторы $X_1, X_2$:
- $X_1$ = `flow_dolphin_vol`
- $X_2$ = `wall_dolphin_vol`

Для модели это **ортогональные переменные**. Она сама найдёт зависимость (например: "Если Wall растёт, а Flow падает → разворот").

**РИСК:** Только если вы случайно подадите `cvd` туда, где ожидается `wall` при подготовке датасета.

---

### ВОПРОС 4: Нужны ли префиксы aggressor_dolphin_cvd vs wall_dolphin_volume?

**ОТВЕТ:** ДА, ОБЯЗАТЕЛЬНО.

Используйте **семантическое именование** в SmartCandle и в БД.

**Предлагаемая схема (Naming Convention):**

| Сущность              | Префикс  | Пример названия поля   | Смысл                        |
|-----------------------|----------|------------------------|------------------------------|
| Удар (CVD/Flow)       | `flow_`  | `flow_dolphin_cvd`     | Агрессия дельфинов           |
| Стена (Iceberg)       | `wall_`  | `wall_dolphin_vol`     | Пассивный объем дельфинов    |
| Ликвидность (Orderbook)| `book_` | `book_dolphin_depth`   | Глубина стакана дельфинов    |

---

## 🛠️ ИТОГОВЫЙ ПЛАН ДЕЙСТВИЙ

### 1. В `domain.py` (IcebergQualityTags):

```python
@dataclass
class IcebergQualityTags:
    is_whale: bool = False      # > $100k
    is_dolphin: bool = False    # $1k - $100k (RENAME: было is_shark)
    # is_shark: bool удаляем
```

---

### 2. В `domain_smartcandle.py` (SmartCandle model):

**ТЕКУЩАЯ СХЕМА (ПРОБЛЕМНАЯ):**
```python
class SmartCandle(BaseModel):
    # AGGRESSORS (без префикса - путаница!)
    whale_cvd: float
    minnow_cvd: float
    dolphin_cvd: float
    
    # WALLS (отсутствуют!)
    # ❌ Нет wall метрик
```

**НОВАЯ СХЕМА (РЕКОМЕНДОВАННАЯ GEMINI):**
```python
class SmartCandle(BaseModel):
    # AGGRESSORS (Молоток) - с префиксом flow_
    flow_whale_cvd: float
    flow_dolphin_cvd: float
    flow_minnow_cvd: float
    
    # WALLS (Наковальня) - с префиксом wall_
    wall_whale_vol: float      # Новое!
    wall_dolphin_vol: float    # Новое! (бывший shark volume)
    # wall_minnow_vol обычно не бывает (шум)
```

---

### 3. В `market_metrics_full` (таблица БД):

**ТЕКУЩИЕ КОЛОНКИ (ПРОБЛЕМНЫЕ):**
```sql
whale_cvd_delta NUMERIC,
minnow_cvd_delta NUMERIC,
dolphin_cvd_delta NUMERIC
```

**НОВЫЕ КОЛОНКИ (РЕКОМЕНДОВАННЫЕ):**
```sql
-- AGGRESSORS (Flow)
flow_whale_cvd_delta NUMERIC,
flow_dolphin_cvd_delta NUMERIC,
flow_minnow_cvd_delta NUMERIC,

-- WALLS (Iceberg detected volumes)
wall_whale_vol NUMERIC,
wall_dolphin_vol NUMERIC
```

---

### 4. В `smart_candles` (таблица БД):

**ТЕКУЩИЕ КОЛОНКИ:**
```sql
whale_cvd NUMERIC,
minnow_cvd NUMERIC,
dolphin_cvd NUMERIC
```

**НОВЫЕ КОЛОНКИ:**
```sql
-- AGGRESSORS
flow_whale_cvd NUMERIC,
flow_dolphin_cvd NUMERIC,
flow_minnow_cvd NUMERIC,

-- WALLS
wall_whale_vol NUMERIC,
wall_dolphin_vol NUMERIC
```

---

## 📊 IMPACT ANALYSIS (Что нужно изменить)

### ЗАТРОНУТЫЕ ФАЙЛЫ:

1. **domain.py**
   - `IcebergQualityTags`: `is_shark` → `is_dolphin`

2. **domain_smartcandle.py**
   - Переименовать: `whale_cvd` → `flow_whale_cvd`
   - Переименовать: `dolphin_cvd` → `flow_dolphin_cvd`
   - Переименовать: `minnow_cvd` → `flow_minnow_cvd`
   - Добавить: `wall_whale_vol`, `wall_dolphin_vol`

3. **migrations/003_create_smart_candles_table.sql**
   - Переименовать колонки

4. **migrations/004_add_smartcandle_columns.sql**
   - Переименовать `dolphin_cvd_delta` → `flow_dolphin_cvd_delta`

5. **candle_materializer.py**
   - Обновить SQL aggregation queries
   - Обновить mapping в SmartCandle objects

6. **repository.py**
   - `get_materialized_candles()`: обновить SQL SELECT
   - Все другие методы работающие с CVD

7. **analyzers.py**
   - `WhaleAnalyzer`: метрики должны писаться как `flow_*_cvd_delta`

---

## ⚠️ BREAKING CHANGES

**КРИТИЧНО:** Это Breaking Change для всей системы!

**ПОСЛЕДСТВИЯ:**
- Все существующие данные в `market_metrics_full` используют старые имена
- Все тесты сломаются
- Нужна миграция данных (ALTER TABLE RENAME COLUMN)

---

## 🎯 ВАРИАНТЫ ДЕЙСТВИЙ

### ВАРИАНТ A: Исправить сейчас (до backfill) ✅ РЕКОМЕНДУЮ

**ПЛЮСЫ:**
- Правильная архитектура с самого начала
- Нет legacy данных с плохими именами
- Проще для будущих ML моделей

**МИНУСЫ:**
- Требует времени (~1-2 часа)
- Нужно пересоздать миграции 003, 004
- Нужно обновить все файлы

**ДЕЙСТВИЯ:**
1. Создать миграцию 005: RENAME старых колонок
2. Обновить domain_smartcandle.py с правильными именами
3. Обновить candle_materializer.py
4. Обновить repository.py
5. Запустить миграцию
6. Запустить backfill с НОВОЙ схемой

---

### ВАРИАНТ B: Отложить на потом (backfill сейчас) ⚠️ НЕ РЕКОМЕНДУЮ

**ПЛЮСЫ:**
- Быстрее (backfill сейчас)
- Откладываем рефакторинг

**МИНУСЫ:**
- Создаст legacy данные с плохими именами
- Потом придётся мигрировать данные (сложнее)
- ML модели будут путаться (когнитивный диссонанс)

**ДЕЙСТВИЯ:**
1. Backfill с текущими именами
2. TODO: Рефакторинг позже (с миграцией данных)

---

### ВАРИАНТ C: Компромисс (aliases в Pydantic) 🤔

**ИДЕЯ:** Добавить алиасы в SmartCandle model:

```python
class SmartCandle(BaseModel):
    # NEW names (for code)
    flow_whale_cvd: float = Field(alias='whale_cvd')
    flow_dolphin_cvd: float = Field(alias='dolphin_cvd')
    
    # DB uses old names, code uses new names
```

**ПЛЮСЫ:**
- Минимальные изменения в БД
- Код использует правильные имена

**МИНУСЫ:**
- Костыль, не решает проблему в БД
- Путаница остаётся в SQL queries

---

## 💡 РЕКОМЕНДАЦИЯ CLAUDE

**Я рекомендую ВАРИАНТ A** (исправить сейчас).

**ПОЧЕМУ:**
- Gemini прав: когнитивный диссонанс — реальная проблема
- У нас ещё НЕТ данных в `market_metrics_full` (таблица пуста)
- Сейчас самое дешёвое время для рефакторинга
- После backfill будет сложнее (нужна миграция данных)

**ПЛАН:**
1. Создам миграцию 005 для RENAME
2. Обновлю domain_smartcandle.py
3. Обновлю candle_materializer.py
4. Обновлю repository.py
5. Применю миграции
6. Backfill с правильными именами

**ВРЕМЯ:** ~30-45 минут работы + 10-15 минут backfill.

**ИТОГО:** ~1 час, но архитектура будет "пуленепробиваемой" (слова Gemini).

---

**Твой выбор?** A, B или C? 🎯
